{
  "name": "chingu_weather_cli",
  "version": "1.0.0",
  "description": "CLI application that retrieves weather informations based on user input",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Marco Ruggeri",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.0.0",
    "dotenv": "^8.2.0",
    "node-fetch": "^3.1.1"
  }
}
